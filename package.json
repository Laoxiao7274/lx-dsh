{
  "name": "lx-dsh",
  "productName": "LX-DSH",
  "version": "0.3.6",
  "private": true,
  "type": "commonjs",
  "description": "LX-DSH — native desktop client for DeepSeek Harness (dsh web)",
  "author": "Laoxiao7274",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/build.mjs && electron .",
    "spike": "node scripts/wire-spike.mjs",
    "locate": "node shared/find-dsh.mjs",
    "icon": "node scripts/make-icon.mjs",
    "round-icon": "node scripts/make-rounded-icon.mjs",
    "dist": "npm run build && node scripts/assemble-dist.mjs && electron-builder --win --dir && electron-builder --win nsis --prepackaged dist/win-unpacked",
    "dist:fast": "npm run build && node scripts/assemble-dist.mjs && electron-builder --win --dir && node scripts/make-portable.mjs",
    "dist:update": "npm run build && node scripts/assemble-dist.mjs && electron-builder --win --dir && node scripts/build-update.mjs",
    "dist:full": "npm run build && node scripts/assemble-dist.mjs && electron-builder --win --dir && electron-builder --win nsis --prepackaged dist/win-unpacked && node scripts/build-update.mjs && node scripts/make-portable.mjs",
    "assemble": "node scripts/assemble-dist.mjs",
    "publish": "node scripts/publish.mjs"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "build": {
    "appId": "com.lxin666.lx-dsh",
    "productName": "LX-DSH",
    "directories": {
      "output": "dist"
    },
    "compression": "store",
    "files": [
      "dist-electron/**",
      "ui/dist/**",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico",
      "signAndEditExecutable": true,
      "signExecutable": false
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false
    },
    "publish": {
      "provider": "generic",
      "url": "http://123.57.129.111/win"
    },
    "afterPack": "scripts/after-pack.cjs"
  }
}
