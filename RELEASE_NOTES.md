# LX-DSH 0.3.5 更新日志

- 全新「快问快答」：侧边栏底部的 ⚡ 按钮从右侧展开抽屉面板——不用切换会话、不打断当前对话，即问即答（搜索先行、回答附来源链接）
- 抽屉内与主聊天同款呈现：思考过程折叠行（可展开查看完整推理）、搜索工具动态、完整 Markdown 排版（标题、列表、可点链接、代码块、表格）
- 「换个问题」一键开新线程；回答流式渲染

# LX-DSH 0.3.4 更新日志

- 更新体验重做：发现新版本不再于后台偷偷下载；在更新弹窗点击「立即安装」才开始下载，进度条就地显示，下载完成后自动安装并重启应用
- 会话日志导出移至轨迹页：导出按钮挪到轨迹工具栏右侧（搜索框旁），会话头恢复纯窗口控制
- 修复：安装包缺少 app-update.yml 导致应用内更新在部分机器上始终无法安装（上一版修复了服务端路径，本版补齐安装包本体，此后新安装不再需要手工补文件）

# LX-DSH 0.3.3 更新日志

- 同步上游 DeepSeek Harness 0.1.2 全量更新（约 1300 项提交）：运行时升级到 Cordis 4，会话控制器与远程协议重写，整体稳定性与性能显著改进
- 上游带来的新能力：消息支持附加图片、会话全文本搜索、聊天与轨迹视图拆分重构
- 对话：新增「撤回到此消息」——鼠标悬停用户消息即可一键撤回该轮对话（含确认弹窗），撤回后视图自动刷新
- 轨迹页 v4：轮次头、步骤分隔行、请求编号、时延与 token 指标条全新视觉；视图入场、详情面板滑入、工具栏交错等动效
- 修复：中文界面下轨迹页步骤行与请求标记全部缺失（本地化判别改为数据驱动）
- 修复：未打开会话时右上角窗口按钮消失、窗口无法拖动——首页保留完整窗口控制与顶部拖拽带，切换不再跳变
- 修复：侧边栏「设置」按钮宽度塌缩
- 修复：应用内更新的安装入口失效（更新服务缺少 electron-updater 清单路径）

# LX-DSH 0.3.2 更新日志

- 架构简化：dsh 运行时作为普通文件直接内置于安装包（resources/dsh），后端直接从安装目录启动——彻底移除「首次启动解压」环节
- 随之消失的问题：首次启动 30-60 秒解压等待、解压中断导致的空运行时、以及由此引发的旧版回退
- 修复：打包部署树缺失 24 个 workspace 依赖包（cosmokit、schemastery 等）导致后端无法启动——构建流程现在扫描全部引用并自动补齐，且发布前强制启动冒烟测试
- 修复：应用内更新缺少 app-update.yml 导致自动更新不可用
- 日志文件按 16MB 轮转，不再无限增长

# LX-DSH 0.3.1 更新日志

- 修复：升级后界面仍是旧版（DeepSeek Harness 旧后端兜底导致改动不生效）——打包版不再静默回退到全局旧安装
- 修复：首次解压运行时失败时给出明确错误信息（不再静默留下空目录）
- 启动页副标题改名

# LX-DSH 0.3.0 更新日志

- 全新 LX-DSH 桌面体验：无边框窗口、自定义窗口控制与拖拽区域
- 应用内自动更新：绿色更新按钮、更新日志弹窗、下载进度与一键安装
- 设置：提供商与模型目录改版；新增每模型视觉能力开关
- 轨迹视图 v4：轮次/步骤结构行、指标尾注、虚拟滚动性能优化
- 会话日志自动修复：损坏的日志在读取时自动截断恢复，不再打不开会话

# LX-DSH 0.2.2 更新日志

- 修复若干稳定性问题
