# LX-DSH 0.3.1 更新日志

- 修复：升级后界面仍是旧版（DeepSeek Harness 旧后端兜底导致改动不生效）——打包版不再静默回退到全局旧安装
- 修复：首次解压运行时失败时给出明确错误信息（不再静默留下空目录）
- 启动页副标题更名
- 本版本必须更新：0.3.0 的安装包内含上述打包缺陷

# LX-DSH 0.3.0 更新日志

- 会话自动修复：运行中意外关闭导致的会话日志损坏（帧校验失败、记录撕裂、序号断裂）现在会在打开时自动修复，不再永久打不开
- 修复撤回后重新连接时可能丢失消息的问题（模型目录后缀读取）
- 左上角品牌行显示版本号，发现新版本时右侧出现绿色「更新」按钮
- 更新弹窗：版本对照、更新日志、实时下载进度、一键安装
- 模型设置全面改版：连接字段与模型目录平铺，模型列表改为清单式布局
- 模型可按行声明视觉能力（文本/图片输入）：行内开关 + 容量面板中的视觉开关
- 界面动效全面升级（GSAP）：列表入场、卡片展开、开关切换
- 模型目录等宽字体、悬停高亮等细节打磨
